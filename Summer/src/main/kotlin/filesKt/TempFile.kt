import filesKt.clearCns
import java.util.concurrent.TimeUnit

/*import java.util.*

val timer = Timer()
val task = object: TimerTask(){
    override fun run(){
        ProcessBuilder("clear").inheritIO().start().waitFor()
        tmpFig.print(kotlin.random.Random.nextInt(1,35),kotlin.random.Random.nextInt(1, corY - 5), console)
    }
}
timer.scheduleAtFixedRate(task, 0, 100)*/

//TimeUnit.SECONDS.sleep(10);




/*
console = tmpFig.moveToRight(console)
clearCns()
println(console)
TimeUnit.SECONDS.sleep(1)

tmpConsole = tmpFig.moveToDown(console)
if(tmpConsole != "error") {
    console = tmpConsole
    clearCns()
    println(console)
    TimeUnit.SECONDS.sleep(1)
}else{
    break
}

console = tmpFig.moveToLeft(console)
clearCns()
println(console)
TimeUnit.SECONDS.sleep(1)
*/

/*
    private var fig = "  ▓▓    " + "\n" +
                      "  ▓▓    " + "\n" +
                      "▓▓▓▓▓▓▓▓" + "\n" +
                      "▓▓▓▓▓▓▓▓"

*/